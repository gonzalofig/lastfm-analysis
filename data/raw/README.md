This folder is used by the notebooks to store the raw downloaded Last.fm archive and the extracted TSV files.

On the first run, `00_setup_and_load_data.ipynb` downloads `lastfm-dataset-1K.tar.gz` automatically and extracts the required files here.
